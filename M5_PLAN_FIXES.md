# M5 plan-fix checklist

Captured at the start of M5 (2026-05-05) from a single-agent reading of the
binding context (in this exact order):

  1. `dsa110-rt_revamp_7b1d2669.plan.md` §8.M2-carryover (lines 2198-2261) —
     locked decisions D1-D18 and implementation gotchas F1-F20 inherited
     from M2. Of these, F18 (PyTorch row-major vs cuBLAS column-major
     gather) and F20 (`np.fft.ifft2` (u, v) negation for TMS-canonical
     (l, m) axes) bind any internal imager M5 builds.
  2. `dsa110-rt_revamp_7b1d2669.plan.md` §8 M5 (lines 2312-2341) — the M5
     milestone scope: search_compute service, cube_injection detector
     unit-test injector, and the four DoD benches (search_node_throughput,
     noise_norm_calibration, trigger_emitter_wiring, cube_injection_detector,
     voltage_fixture_search).
  3. `dsa110-rt/PARALLEL_AGENTS.md` — branch / file-ownership / h01-test-
     isolation conventions for M3 ∥ M5 parallel development.

These are inconsistencies / typos / under-specifications in the plan that the
M5 author resolves in code via locked decisions (see "Locked decisions"
below). All plan edits are **deferred to the M5 hardening pass at the end**
(final chunk) so the M5 critical path is not blocked by sequential
plan-edit + scp + relock cycles. This file is retired during M5 hardening
once F-items + D-items have been folded into plan.md.

The list below is the **starting scaffold**; it grows as M5 chunks land
(parallel-explore subagents surface additional F-items at chunk boundaries,
exactly the M2 pattern documented in `M2_PLAN_FIXES.md` provenance).

---

## Locked decisions (apply at chunk-author time)

| ID | Topic | Decision | Source |
|----|-------|----------|--------|
| D1 | Cube-injection scope (plan §8 line 2329) | `bench/cube_injection_detector.py` operates entirely on post-imaging `[T_det, N_fine_DM, N_grid, N_grid] float32` cubes synthesised by `src/dsart/inject/cube_injection.py`; bypasses every upstream stage (no corr, no transport, no RX ring, no fine-DM combiner, no imager). The cube is the **real-valued dirty image** the imager would have produced, not a complex visibility tensor. This decouples M5 detector dev from M3 / M4a entirely until the voltage-fixture gate runs. | Plan §8 line 2329 explicit text "develops the detector independently of every upstream stage". |
| D2 | Detector kernel-bank shape | 4 image kernels × 4 DM kernels × 8 time kernels = **128 kernel triples** at default. Image: `("unit", "psf", "psf_shift_lm", "psf_shift_l")` per `DETECTOR_IMAGE_KERNELS`. DM: `("d1","d3","d5","d7")` per `DETECTOR_DM_KERNELS`. Time: `("b1","b2","b4","b8","b16","b32","b64","b128")` per `DETECTOR_TIME_KERNELS`. Kernel id schema `"k_img:k_dm:k_time"` already enforced by `Candidate._check_kernel_id` (M1 contracts.py). | `configs/config_compute_search.yaml::detector` + `src/dsart/common/constants.py` already pin this. M5 must not re-pin in code; consume the constants. |
| D3 | h01 test-isolation envelope | `DSART_BUFFER_KEY_PREFIX=m5` (so `fada → fa5a`, `bada → ba5a`, `dada → da5a`); `CUDA_VISIBLE_DEVICES=1`; `DSART_ETCD_NAMESPACE_PREFIX=m5`; per-milestone lockfile `/var/lock/dsart-m5.lock`; status JSON at `~/dsart-m5-status.json`; bench reports under `bench/reports/<UTC>/<run_id>/M5/`; operator-approval marker at `bench/reports/M5/m_operator_approved.yaml`. All wired into `tools/dod/M5.sh`. | `PARALLEL_AGENTS.md` §4 (4.1 buffer keys, 4.2 GPU, 4.3 reports, 4.4 lockfile, 4.6 etcd namespace). |
| D4 | M5 voltage-fixture gate fixture | `/home/ubuntu/data/voltages/250924mptq/` — burst, DM ≈ 404.7 pc cm⁻³, RA = 307.78°, Dec = 53.85°, MJD ≈ 60942.172, SNR ≈ 30. Same fixture M3 uses for its burst sub-DoDs (line 2286 / 2291). M5 consumes the M3-emitted captured transport-TX `.npz` set; the gate is the **only** M3 → M5 coupling point. | `PARALLEL_AGENTS.md` §1 + §5; user briefing step 5. |
| D5 | M5 viz helpers location | `tools/viz/common.py` is owned by M3 (M2 hardening retired); M5 puts new viz helpers in `tools/viz/search_helpers.py` (new file). `tools/viz/search_detector_check.py` is the M5-owned operator-facing CLI (mirrors `corr_imager_dedisperser_check.py`'s shape; consumes the M5 cube + emitted candidates + injection log). Edits to `tools/viz/common.py` from M5 only via PR ack'd by M3. | `PARALLEL_AGENTS.md` §3 Class C `tools/viz/common.py`. |
| D6 | Internal imager `(l, m)` sign convention | If M5 ever spins up its own internal imager helper (the production `imager.py` consumes a pre-gridded sparse tensor from M3, but `cube_injection.py` may need to compute predicted `(l, m)` for an injection at known sky coords for the voltage-fixture gate), it inherits the F20 convention from M2 carryover: negate `(u, v)` once at the table-build site so `np.fft.ifft2` (positive-exponent iFFT) lands TMS-canonical `(+l, +m)` axis labels. Document with `# F20` comment. | M2 carryover §3.6.5 / `tools/viz/common.py::grid_uv_natural`; plan §8.M2-carryover line 2228. |
| D7 | Operator-approval marker scheme | Reuse M2's D11 scheme verbatim. Marker file at `bench/reports/M5/m_operator_approved.yaml` with fields `{operator, approval_utc_iso, milestone: M5, voltage_run_id, viz_artifact_sha256}`. M5 has TWO operator gates per plan §8 lines 2329 + 2339: cube-injection detector inspection (no fixture; the synthetic-injection heatmap) and voltage-fixture search (real burst). Convention: a single marker yaml covers both, with `voltage_run_id="cube_injection"` for the synthetic gate and `voltage_run_id="<burst_run_id>"` for the fixture gate. The M5.sh stamp logic mirrors M2.sh (`complete (needs operator approval)` → `complete (approved)` → `complete (hardened)`). | `PARALLEL_AGENTS.md` §4.3 + M2 D11 carryover. |
| D8 | Cube-injection synthetic noise convention | Cube tensor dtype is **float32 real-valued** (the post-imager dirty image); thermal-noise background is iid Gaussian with σ = 1 per cell (so SNR readouts are direct: pixel value / 1.0); injection amplitudes are then injected at the requested SNR directly. The cube does NOT carry a complex visibility — that's the imager's input, not its output. Pin in `inject/cube_injection.py` docstring; record here so the noise-norm bench's analytic FAR `0.5 · erfc(θ/√2)` directly applies cell-for-cell at the cube level. | Plan §8 line 2329 declares the cube shape as `[T_det, N_fine_DM, N_grid, N_grid] float32` (real); the σ=1 normalization is the natural choice for a "develops independently" path. |
| D9 | MockTriggerListener port | `127.0.0.1:11227` per plan §8 line 2328 (trigger_emitter_wiring bench). The cube_injection_detector bench (plan §8 line 2329) implicitly uses the same listener; pin one port for both benches. | Plan §8 lines 2328-2329 cross-reference. |
| D10 | v1 detector image-kernel construction | The four image-kernel namespaces `("unit", "psf", "psf_shift_lm", "psf_shift_l")` (per `DETECTOR_IMAGE_KERNELS`, D2) all ship in v1 as **delta-function kernels** (1×1 singleton, value 1.0). The `kernel_id` namespace is preserved so the cross-kernel merger / decoder / FAR formulas are exercised against the full 128-triple bank, but the PSF-aware variants (`psf*`) are stubbed at delta and marked `# TODO(v2)` in `src/dsart/detector/kernels.py`. Rationale: the cube-injection bench (plan §8 line 2329, primary detector correctness gate) operates on a **post-imager** cube where the PSF is already convolved into the noise; a v1 detector that runs a second PSF convolution on top would double-convolve. The PSF-aware kernels become meaningful only at v2 / hardening when the production `search_compute` (Chunk 6) consumes a pre-imager sparse-COO tensor (M3-emitted) and the detector applies the PSF as a matched filter. v1 deliberately validates the kernel-bank scaffold (Layer-2 σ_k EMA, decoder, merger, emitter) end-to-end against synthetic cubes; v2 lights up the matched-filter math. Plan §8 line 2314 should add a parenthetical "(v1: delta kernels for all four image-kernel slots; v2 introduces matched-filter PSFs)" during hardening. | Operator decision 2026-05-05 (kickoff Q&A, kernel-construction question). |
| D11 | Layer-2 σ_k placeholder during chunks 1-2 (RETIRED Chunk 3) | Until Chunk 3 lands the Layer-2 σ_k EMA, the detector's `s_k` divisor is a deterministic per-kernel constant `s_k = sqrt(k_dm_width × k_time_width)` — the analytic noise std of an unweighted-sum boxcar of `K_dm × K_time` cells over unit-σ Gaussian input. Pin in `src/dsart/detector/forward.py::DeterministicDetector._sigma_k_placeholder()` and document the swap-out point. With the cube-injection bench's σ=1 thermal noise (D8), this placeholder gives `score / s_k ≈ N(0, 1)` on noise-only cubes, so chunk-2 decoder + merger tests at θ=8 fire essentially zero false candidates and the matched-filter recovery math works cell-for-cell. Chunk 3 swaps the placeholder for the EMA-tracked value with no Protocol surface change. **Chunk 3 status**: retired. The Layer-2 EMA (`Layer2State`, plan §3.6.10) replaces the placeholder; the `_sigma_k` registered buffer mirrors the EMA-tracked tensor. The cold-start seed defaults to the analytic placeholder (`layer2_seed_unit=True` in `DeterministicDetector.__init__`) so the very first cube divides by a sensible scalar before the Welford burn-in completes. | Implementation derivation; needed to ship a **fully wired** chunk-2 detector that returns real candidates (not a stub) without blocking on Chunk 3 noise_norm. |
| D13 | Layer-2 EMA cold-start seed | The `Layer2State._s_k` buffer is initialised to `1.0` per kernel by default (canonical Welford). The `DeterministicDetector` constructor passes `layer2_seed_unit=True` (default) which seeds the EMA to the analytic per-kernel divisor `sqrt(k_dm_width × k_time_width)` — the noise std of an unweighted-sum boxcar over unit-σ Gaussian input — so the very first cube emitted (before the Welford burn-in completes) divides by a scalar of the right magnitude rather than 1. Tests / benches that need raw Welford-from-cold can pass `layer2_seed_unit=False`; the `bench/noise_norm_calibration.py` (Chunk 6) does this so the FAR convergence is visible across the burn-in cubes. The seeded value is a starting point, not a fixed offset — the burn-in's running mean replaces it after `noise_layer2_n_burnin` cubes (default 30). | Implementation derivation; gives a sensible cold-start divisor and matches the Chunk-2 behaviour for backwards-compatibility of cube-injection tests. |
| D14 | Layer-2 invalid-cube semantics | Per plan §319, an invalid cube (any `validity_mask` cell False, RFI'd or warmup) skips BOTH `Detector.forward()` and the noise update. Chunk 3 implements: `forward()` still runs the conv-bank + decoder + merger (so the candidate log records what the detector saw, even if a cube was contaminated), but `Layer2State.update_and_query(scores, valid=False)` short-circuits — the EMA tensor is unchanged and `cube_count` does NOT increment, so the burn-in length is correct in calendar-cube terms (a contaminated cube doesn't advance the warmup). The Detector reads `validity_mask` cell-wise and treats the cube as invalid if **any** cell is False (conservative; can be relaxed to per-(t, fdm) later if the bench shows the conservative rule is rate-limiting). Per-(t, fdm) masking is plumbed through to the EMA via a future `validity_mask_per_kernel` argument when the per-kernel σ-clipped reduction can ingest a NaN mask without dropping interior bias. | Plan §319; chunk-3 implementation choice. |
| D12 | Cube-injection profile shape | v1 ships **boxcar (flat-top) injections only**: a temporal pulse of duration `width_samples`, peak amplitude `snr / sqrt(width_samples)` per cell, on top of unit-σ Gaussian thermal noise (D8). Mathematically: a width-`W` matched boxcar applied to a flat-top of duration `W` and amplitude `A` over unit-σ noise gives recovered SNR = `A · √W = snr` exactly, so the bench's "recovered SNR ≥ 0.85 × injected" tolerance (plan §8 line 2329) decomposes cleanly into "cumsum-arithmetic precision + Layer-2 σ EMA jitter". Profile values other than `"boxcar"` (e.g. `"gaussian"`, `"scattered"`) raise `NotImplementedError` in v1; v2 may add them when the voltage-domain injector library schema is finalised. | Implementation derivation; the matched-filter exact-recovery math is the property the operator visually inspects in the SNR-vs-width recovery heatmap (plan §8 line 2329). |
| D15 | Cube-injection bench predicate-chain knobs | The bench at `bench/cube_injection_detector.py` deliberately RELAXES the production trigger-emit predicate chain so the FAR sub-check sees the full noise-only emit distribution: `SnrThreshold = 5.0σ` (vs production 8.0), `PerCubePerKernelCap = 128` (vs 4), `PerCubeTotalCap = 1024` (vs 16), `RateLimitTokenBucket(rate=1e6/s, burst=1e6)` (effectively disabled). Holdoff stays at the production 50 ms (the bench's match-radius helper accepts the holdoff-sized t-window so this does not suppress recoveries). The production knobs are exercised separately by `bench/trigger_emitter_wiring.py` (Chunk 6). Pin in the bench module-level constants `BENCH_*`; production-side `services/search_compute.py` (Chunk 6) reads from `configs/config_compute_search.yaml` and is unaffected. | Implementation derivation: the FAR sub-check needs to see the broad distribution to trace the empirical-vs-analytic curve; production caps would clip the tail and obscure the calibration. |
| D16 | Cube-injection bench recovery match-window | The bench scores a recovery as a "match" when an emitted Candidate falls within `(Δl ≤ 2, Δm ≤ 2, Δfdm ≤ max(2, k_dm/2+1), Δt ≤ max(64, k_time/2+1))` of the injection. The lm window absorbs the local-max NMS spatial radius (plan §1585 default of 2); the fdm window grows with the kernel's K_dm boxcar; the time window grows with K_time. The 64-sample default time window is the K_time=128 boxcar's half-support (plan §1592 time-edge gate masks first/last 64 samples → matched filter can plant the peak anywhere within ~K_time/2 of the injection). The bench's `_kernel_match_radius` helper derives these per-kernel; tighter or looser windows are operator-tunable. Plan §8 line 2329 mentions "(l, m, fine_dm_idx, t)" tolerance in passing but does not pin numeric values; this D-item locks them. | Implementation-time pin; plan should add the per-kernel position tolerances during hardening. |
| D17 | Bank-mask CLI for both detector benches | `bench/cube_injection_detector.py` and `bench/search_node_throughput.py` accept `--bank-mask "k_img=<tokens>;k_dm=<tokens>;k_time=<tokens>"` (parser shared in `bench/_bank_mask.py`). Each axis defaults to `"*"` (= keep all); unspecified axes inherit the full default. Operators sweep K_img × K_dm × K_time bank configurations at a fixed cube geometry to characterize the perf-vs-recovery Pareto. The throughput bench *also* takes `--n-grid` (already pinned in chunk 6b-β) so the operator can additionally sweep the imager FFT cost. Plan §8 line 2318 ("if 8 cubes/s is not reachable, the lead-agent reduces detector_K_img × K_dm × K_time per §9") becomes operationally measurable through this CLI. | Chunk 6c-α + 6c-β implementation; tools/viz/search_perf_characterization.py (Chunk 6c-γ) consumes the resulting summary.json files to render the Pareto report. |
| D18 | v1 K_img + K_dm degeneracy at on-grid injection (empirical, Chunk 6c) | Empirical Chunk 6c data (h01 GPU 1; cube_injection 4 mask configs; 16 cells per config) confirms what D10 implied: at v1 with delta image kernels and on-grid (fine_dm_idx-aligned) boxcar injections, the per-cell `snr_ratio_mean` is **identical** across `{full, k_img=unit, k_dm=d1, k_img=unit;k_dm=d1}` to 4 decimal places. The K_img dimension is degenerate (all four kernels are 1×1 deltas per D10) and the K_dm dimension is degenerate when the injection lands cleanly on a fine-DM trial (the K_dm boxcars only earn their keep when DM smearing > fine-DM trial spacing). Production decision: the v1 production bank can be safely collapsed to `k_img=unit; k_dm=d1` (8 kernels) **for on-grid pulses**; off-grid characterization (DM uncertainty ≫ trial spacing) is a follow-up that would re-engage K_dm. K_time stays full per the matched-filter argument. **Caveat**: this collapse is a v1 statement only; v2 (matched-filter PSFs + DM-smeared injections) re-opens the question. | Chunk 6c empirical sweep; report at `bench/reports/<UTC>/perf_char/M5/report/`. |
| D19 | Imager-only GPU throughput, cuFFT-cfp16 (Chunk 6c follow-up, performance hardening) | The chunk-6a numpy/CPU combiner+imager at `image/imager.py` is a placeholder per its own docstring (lines 18-24: "production should be a torch-cuda imager"). The chunk-6c-follow-up bench `bench/imager_only_gpu.py` measures the production-target GPU path — cint8 → cfp16 dequant on GPU + per-fdm in-place `add_` accumulation across 16 chgroups + cuFFT-cfp16 `ifft2` + fftshift + edge mask — at the geometry sweep operator pinned in 2026-05-06 chat (T_det ∈ {512, 384, 256, 192, 128} at fixed N_fdm=32 / N_grid=256, cube_dtype=cfp16). Headline result on h01 GPU 1 (RTX 2080 Ti, 11 GiB): **T_det=512 → 1.81 cubes/s; T_det=384 → 2.39; T_det=256 → 3.52; T_det=192 → 4.62; T_det=128 → 6.69** (python_addloop baseline). Per-cube cost is dominated by the 16-chgroup index-shifted sum (combine: ~70% of cube time) which is memory-bandwidth bound (∼510 GB/s effective vs ∼616 GB/s peak HBM, so a ~3× headroom remains via a fused single-pass CUDA kernel). cuFFT-cfp16 itself is well-tuned: 86 ms for 16384 (32×512) 256² FFT2s at T=512 ≈ 5.2 µs / FFT. cfp32 vs cfp16 measured at 524 vs 283 ms / cube at T=256 (exact 2× memory traffic, exactly as predicted). Memory budget at production geometry fits under 6 GiB peak (1.25 GiB cint8 streams + 2.5 GiB cfp16 dequant + 2 GiB output cube + 2.5 GiB transient, all under PYTORCH_CUDA_ALLOC_CONF=expandable_segments). The production combiner+imager at `image/imager.py` should be ported to this path during M5 hardening (chunk 8). Plan §8 line 2317 ("≥ 8 cubes/s") becomes operationally measurable: see D20 for the fused-kernel follow-up that achieves the target. | Chunk 6c follow-up; bench at `bench/imager_only_gpu.py`; sweep report at `bench/reports/20260506T172631Z/imager_gpu/M5/`. |
| D21 | Fused dequant+combine cint8-input CUDA kernel (Chunk 8, **plan §8 8-cubes/s target achieved at T_det=256, the operator-pinned deployment integration time**) | D20 measured the chunk-6c fused combine kernel at T_det=256 at 5.93 cubes/s — short of the 8 cubes/s plan target by ~25%. The remaining cost was concentrated in the bench's "scatter" step (35.6 ms, a 4-pass dequant: cint8→fp32→permute→contiguous→cfp32→cfp16) plus the chunk-6c combine kernel reading 4-byte cfp16 cells when the underlying SparseCOOPayload (D2/M1) is 2-byte cint8. Chunk-8 fuses the dequant directly into the combine kernel: a new NVRTC kernel `fused_dequant_combine_per_fdm_cint8_to_cf{16,32}` reads cint8 streams in their native `[N_chgroup, T_stream, 2, N_grid, N_grid]` split-plane re/im layout (matching M3 wire payload), accumulates per-fdm across the 16 chgroups in int32 registers (exact for N_chg ≤ 16, max sum ±2032), and writes the cfp16 uv slab in one kernel pass. The bench's scatter step is omitted entirely. **Headline production-geometry results (h01 GPU 1, fused_cuda_cint8, N_fdm=32, N_grid=256, cfp16 output): T_det=256 → 9.79 cubes/s ✅** (clears 8 cubes/s with 22% headroom — the operator-pinned T_det for v1 deployment); T_det=192 → 12.62 cubes/s; T_det=128 → 18.18 cubes/s. The cint8-fused kernel is **2.78× faster than python_addloop and 1.65× faster than chunk-6c fused_cuda** at T_det=256. **The kernel is also strictly more numerically accurate than either reference**: int32 accumulation is exact (vs ~16 ULP fp16 reduction error in the chained references) and the single fp16 cast at the end is ≤ 1 ULP. cint8-to-fp16/fp32 cast is exact since cint8 [-127, 127] fits in the 7+ bit fraction of either dtype. Tests at `tests/test_fused_combine_cuda.py` include a bit-exact cf32 reference test (passes with diff = 0) plus 4 cint8-specific edge cases (zero shifts, dtype validation, layout validation, 4-ULP cf16 atol vs cf32-then-cast); 10/10 pass on h01. After fusing, per-cube cost at T_det=256 is split fifty-fifty between combine (43.6 ms, 45%) and ifft2 (43.2 ms, 44%), with mask the remainder (10.9 ms, 11%); cuFFT-cfp16 is now the binding constraint at the 5.2 µs/FFT2 near-optimal ceiling. Plan §8 §8 cubes/s target met at **every T_det ≤ 256** when paired with D18's collapsed 8-kernel detector bank: T=128 → 18.5 cubes/s end-to-end; T=192 → 12.6 cubes/s; T=256 → 9.6 cubes/s. Chunk-8 remaining items: (a) port `image/imager.py` from the chunk-6a numpy placeholder to this GPU path; (b) verify M3 → M5 RX-ring delivers cint8 streams pre-staged on GPU; (c) bake per-block scale/offset floats into the kernel cast (production uses per-chgroup (scale, offset); bench uses unit scale = pessimistic). | 2026-05-06 operator-prioritised chunk-8 hardening (T_det=256 target); module at `src/dsart/image/fused_combine_cuda.py` (`fused_dequant_combine_per_fdm`); CLI `--combine-impl fused_cuda_cint8` (default) at `bench/imager_only_gpu.py`; tests at `tests/test_fused_combine_cuda.py` (10/10 pass on h01); sweep report at `bench/reports/20260506T212501Z/imager_gpu/M5/`. |
| D20 | Fused per-fdm combine CUDA kernel (Chunk 6c follow-up, **plan §8 8-cubes/s target achieved at T_det=128**) | D19 measurement showed the 16-chgroup index-shifted combine sum was the binding cost (~70% of cube time) at ~510 GB/s effective vs ~616 GB/s peak HBM. Operator-prioritised fusion landed in `src/dsart/image/fused_combine_cuda.py`: a custom CUDA kernel JIT-compiled via cupy's NVRTC (`cupy.RawKernel`, **not** PyTorch's `torch.utils.cpp_extension.load_inline` — the latter requires a host gcc, and h01 is in a compiler-version sandwich where system gcc 7.5 is too old for PyTorch ABI, conda-forge gcc 15 emits C++23 `_Float64` types nvcc 11.8 can't parse, gcc 10 has std::tuple template-pack regressions in newer pybind11, and gcc 11 hits `std::is_same_v` parse errors with the libstdc++ shipped). NVRTC compiles the kernel directly from CUDA source, no host gcc involved, no version dance. Torch tensors are passed zero-copy via `cupy.cuda.UnownedMemory` views; cfp16 is reinterpreted as uint32 because cupy 13.x's complex32 dtype isn't binary-compatible with torch.complex32 across cupy/torch versions, but the kernel re-casts via `__half2*` internally so the bytes flow correctly. The kernel reads each chgroup once and writes the output once: memory traffic drops from `N_fdm × (1 + 16 × 3) × T_det × N²` cfp16 (49× slab volume) to `N_fdm × (16 + 1) × T_det × N²` cfp16 (17× slab volume), measured 2.7× combine speedup at every geometry on the 2080 Ti. Headline production-geometry results (h01 GPU 1, cfp16, N_fdm=32, N_grid=256, fused_cuda): **T_det=128 → 10.74 cubes/s ✅ (clears 8 cubes/s); T_det=192 → 7.71 cubes/s ⚠️ (within 4%); T_det=256 → 6.01 cubes/s; T_det=512 → 3.19 cubes/s** — vs D19 baselines of 6.69 / 4.62 / 3.52 / 1.81 cubes/s. Combined with D18 (collapsed bank to 8 kernels, ~3-13 ms detector cost), **plan §8's 8-cubes/s target is met at T_det=128 with ~25% headroom** (89 ms imager + 3 ms detector = 92 ms / cube → 10.9 cubes/s). T_det=192 is borderline (7.7 cubes/s). T_det=256 and above require additional hardening (next item: fuse cint8 dequant into the combine kernel, ~2× projected win on streams memory traffic since cint8 is half the size of cfp16). Build-system one-time setup on h01: `pip install ninja` (only required for the legacy cpp_extension path which we no longer use; cupy's NVRTC needs no extra deps). The dsart-build conda env (gcc 11) is also retired by the cupy pivot — cleanup item for chunk 8. All five numerical-equivalence + edge-case tests pass: cf32 atol=1e-5, cf16 atol=0.05 vs Python add-loop reference. **Chunk 8 hardening item**: port `image/imager.py` to use the fused kernel; fuse cint8 dequant into the combine kernel for T_det=256 to clear 8 cubes/s; verify M3 → M5 RX-ring delivers cint8 streams on GPU (no host→device transfer in production). | 2026-05-06 operator-prioritised chunk-6c follow-up; module at `src/dsart/image/fused_combine_cuda.py`; tests at `tests/test_fused_combine_cuda.py` (5/5 pass on h01); sweep report at `bench/reports/20260506T191520Z/imager_gpu/M5/`. |

---

## Plan-fix items (apply during M5 hardening)

| ID | Section | Fix | Source |
|----|---------|-----|--------|
| F1 | §8 line 2315 | Plan refers to `tools/build_injection_library.py` schema for cube-injection config field overlap, but the schema itself is not pinned in §3 / §3.1 (the contracts section). Cube-injection config dataclass needs a frozen contract analogous to `Candidate` — fields `(l, m, fine_dm_idx, t_in_cube, snr, profile, width_samples)` plus a serialiser to/from a yaml/npz library. M5 chunks pin the dataclass in `src/dsart/inject/cube_injection.py` and document the schema; plan §3.1 / §3 should grow a `CubeInjectionConfig` contract entry during hardening. | Author from plan §8 line 2315 + `tools/build_injection_library.py` reference. |
| F2 | §8 line 2329 | Plan declares the cube as `[T_det, N_fine_DM, N_grid, N_grid] float32` but does not say it's real-valued (it IS — the cube is the post-imager dirty image, not a complex visibility tensor). D8 above pins `float32 real, σ=1 thermal noise per cell`; plan §8 line 2329 should add a parenthetical "real-valued; σ=1 per cell after Layer-1 normalization" to remove the ambiguity. | Implementation-time clarity; downstream consumers (`Detector.forward` Protocol) need to know the input is real to size their fp16 accumulators correctly. |
| F3 | §8 line 2329 | Plan's cube_injection FAR check ("FAR on the noise-only background within [0.5×, 2.0×] of analytic at θ=8") does not pin **N_eff** (effective number of independent cells per cube) for the analytic Gaussian-tail formula. For a cube of `T_det × N_fine_DM × N_grid × N_grid` cells under a kernel triple of `(K_img, K_dm, K_time)` boxcar volume, `N_eff_per_cube_per_kernel ≈ (T_det × N_fine_DM × N_grid² ) / (K_img · K_dm · K_time)` summed over the 128 triples gives the per-cube expected count at threshold θ. Pin this formula in `noise_norm/` docstrings + the FAR-check helper; plan should add it as a footnote during hardening. | Implementation derivation; needed for both `bench/noise_norm_calibration.py` and the cube_injection_detector bench's FAR sub-check. |
| F4 | §8 lines 2316-2329 | Plan lists 4 DoD benches but does not say which are mandatory for M5 closure when M3 / M4a are not yet done. Per `PARALLEL_AGENTS.md` §1, only `voltage_fixture_search.py` (line 2330) depends on M3. The other three benches plus the new `cube_injection_detector.py` (the user-flagged primary detector correctness gate per the briefing's step 5 + plan §8 line 2329 explicit "develops the detector independently of every upstream stage") are M5-internal and run h01 alone. Resolution: M5 can land all four non-fixture benches before M3 / M4a finish; M5 closure waits on the voltage-fixture gate operator sign-off. M5.sh's status JSON tracks `chunks_complete / total_chunks` so M3 / Ops can see the partial-progress state. | Briefing step 5 + plan §8 line 2329 explicit independence claim + `PARALLEL_AGENTS.md` §1. |
| F5 | §8 line 2329 | Plan says `MockTriggerListener` on `127.0.0.1` for cube_injection_detector but does not pin the port (the trigger_emitter_wiring bench at line 2328 uses `127.0.0.1:11227`). Resolution: same listener, same port for both benches (D9 above). Plan should pin `127.0.0.1:11227` at line 2329 during hardening. | Cross-reference between lines 2328 + 2329. |
| F6 | §8 line 2330 | Voltage-fixture gate requires the M3 captured transport-TX `.npz` set. The `.npz` contract (per-`chgroup` filename scheme, dtype, axis order, manifest fields) is implicit in plan §8 line 2291 ("16 captured transport-TX `.npz` files") but not pinned. M3's `bench/voltage_fixture_fast_corr_burst.py` will produce them; M5's consumer reads them. The `.npz` schema is a Class B integration contract between M3 and M5. Resolution: M3 publishes the schema in `M3_PLAN_FIXES.md` (or a new `src/dsart/transport/captured_npz.py` helper); M5 imports the helper read-only. Pin in plan §8 lines 2291 + 2330 cross-reference during hardening. | M3 ↔ M5 coupling point identified in `PARALLEL_AGENTS.md` §1. |
| F7 | §8 line 2314 | Plan describes the v1 detector as "deterministic conv bank with K_img×K_dm×K_time = 128 default kernel triples" without distinguishing whether the four `DETECTOR_IMAGE_KERNELS` slots ship as PSF-aware matched filters or as delta-function placeholders. D10 above pins v1 = delta-only across all four image-kernel slots (the cube-injection bench operates on a post-imager cube where PSF is already convolved in; matched-filter PSFs are a v2 deliverable that lights up when the production `search_compute` consumes pre-imager sparse-COO tensors). Plan should clarify the v1 / v2 split during hardening. | D10 + plan §8 line 2329 cube-injection bench scope (post-imager input). |
| F8 | §3 line 94 vs `PARALLEL_AGENTS.md` §3 | Plan §3 line 94 lists the detector subpackage as `interface.py / v1_deterministic.py / decoder.py / noise_norm.py`; `PARALLEL_AGENTS.md` §3 (Class A files M5 owns) lists `forward.py / decoder.py / merger.py / kernels.py`. The two decompositions disagree on file names. Resolution: M5 follows the **PARALLEL_AGENTS naming** because it is the binding ownership spec for parallel-agent coordination. The mapping is: plan's `interface.py` + `v1_deterministic.py` collapse into PARALLEL_AGENTS' `forward.py` (Detector Protocol + the v1 deterministic conv-bank Module satisfying it) plus a new `kernels.py` (the K=128 kernel-bank construction); plan's `decoder.py` is split into PARALLEL_AGENTS' `decoder.py` (per-kernel local-max NMS + canonical-zone emit gate) plus a new `merger.py` (cross-kernel SNR-sort + 4D merge-radius suppression); plan's `noise_norm.py` becomes PARALLEL_AGENTS' `noise_norm/{layer1,layer2}.py` directory split (per-layer testability). Additional consequence: `configs/config_compute_search.yaml::detector_class` currently points at `dsart.detector.v1_deterministic.DeterministicDetector` (the plan's name); update to `dsart.detector.forward.DeterministicDetector` in chunk 1. Plan §3 line 94 should be updated during hardening to match. | Plan §3 line 94 vs `PARALLEL_AGENTS.md` §3 (Class A); resolved in Chunk 1. |
| F9 | §3.6.13 line 1131 vs §4.4 line 1588 | Plan §3.6.13 clause (a) text-as-written forbids `F.max_pool1d`-with-stride-1-and-kernel>1 in `decoder.py`, but plan §1588 prescribes exactly that — `F.max_pool3d` on (fdm, l, m) followed by `F.max_pool1d` on time — as the per-kernel local-max NMS implementation. The two are reconcilable on intent: §3.6.13's pin targets the *naive width-by-width sum form* of the K_dm/K_time boxcar that would burn 10× the FLOPs of the cumsum-difference form (`F.conv1d` and `F.avg_pool1d` are the obvious offenders; `F.max_pool1d` is structurally not a sum substitute and can't replace the cumsum trick anyway). Resolution: the AST scanner in `tests/test_detector_protocol.py` permits `F.max_pool1d` / `F.max_pool3d` in `decoder.py` but still forbids `F.conv1d` / `F.avg_pool1d` there; in `forward.py` / `kernels.py` (where the conv bank lives) all three remain forbidden. Plan §3.6.13 line 1131 should narrow the forbidden set to `{conv1d, avg_pool1d}` for `decoder.py` during hardening. | Plan §3.6.13 line 1131 vs §4.4 line 1588; resolved in Chunk 2 (decoder NMS path). |
| F10 | §8 line 2329 cube-injection coord convention | Plan §8 line 2329 describes the cube-injection config as `(l, m, fine_dm_idx, t_in_cube, snr, profile, width_samples)`. The cube-level injector operates **in cube-pixel space** (no projection from sky cosines to pixels — that's the imager's job, which is bypassed per D1). Resolution: the `CubeInjectionConfig` dataclass uses fields `l_pix: int` and `m_pix: int` (integer pixel indices into the `[N_grid, N_grid]` cube spatial axes) with the convention `(l_pix, m_pix) = (N_grid//2, N_grid//2)` is the phase center. A separate classmethod `from_lm_radians(l_rad, m_rad, n_grid, pixel_scale_rad)` converts from the (sky-cosine) (l, m) used elsewhere in the plan; the bench's voltage-fixture cross-check (chunk 7) wires the conversion explicitly. Plan §8 line 2329 should clarify "(l, m here are integer cube-pixel indices, distinct from sky-cosine (l, m) used in §3.6.5 / §4.4)" during hardening. | Implementation-time clarity; downstream bench tooling (`tools/viz/search_detector_check.py --mode cube_injection`) must distinguish pix vs cosine to render heatmaps without an off-by-one bug. |
| F11 | §8 line 2329 noise normalisation | Plan §8 line 2329 says the cube is `float32` thermal noise + injection but does not pin the noise std. D8 above pins σ=1 per cell. The `Detector.forward()` Protocol however expects the cube to be **post-Layer-1-normalised** (plan §3.6.10 + §3.6.13), i.e. each `(t, fdm)` slice already divided by the per-cube σ-clipped global scalar from `noise_norm/layer1.py`. For the cube-injection bench, σ=1 per cell satisfies "post-Layer-1" trivially (Layer-1 is a no-op on already-unit-σ data); for tests that invoke the detector directly on a cube_injection-produced cube, no Layer-1 wrap is needed. Pin in `inject/cube_injection.py` docstring + `noise_norm/layer1.py` cross-reference (when chunk 3 lands). Plan §8 line 2329 should add "(cube is already in post-Layer-1 normalised units; the per-cube σ-clipped scalar is unity by construction)" during hardening. | Implementation derivation; clarifies the σ_layer1 argument to `Detector.forward()` for unit tests (it is unit-valued for cube_injection cubes). |
| F12 | §8 line 2329 + §4.7 viz tools | Plan §4.7 / §8 line 1880-1887 lists `tools/viz/search_detector_check.py --mode cube_injection` outputs (recovery heatmap, score_per_kernel, candidates, FAR plot, report.html) but doesn't pin which tool produces the per-injection NDJSON log nor its schema. Resolution: `bench/cube_injection_detector.py` (Chunk 5) is the producer; the log is `injection_log.ndjson` (one record per (snr, width) cell with fields `{kind: "injection", injected: {…}, n_trials, n_recovered, recovered_snrs, matched_kernel_id, score_per_kernel_at_match: {kernel_id → snr}, cube_geometry: {T_det, N_fdm, N_grid}}`) plus a sibling `noise_only_log.ndjson` (one record per noise-only cube with `{kind: "noise_only", cube_id, candidate_snrs, cube_geometry}`) plus a `summary.json` (config snapshot + cells + far table). The viz CLI (`tools/viz/search_detector_check.py`) consumes all three. Plan §8 line 2329 should pin this contract during hardening so v2 viz tools (or a v2 reference detector) can produce the same logs. | Implementation derivation; defines the bench↔viz integration contract. |
| F13 | §8 line 2329 FAR validation rigour | Plan §8 line 2329 specifies "FAR within [0.5×, 2.0×] of analytic at θ=8 across 30 s of synthetic cubes". With `T_det=512 × N_fdm=8 × N_grid²=64²` cells and the 128 kernel triples, the per-cube expected count at θ=8 is ≈ N_eff × 0.5·erfc(8/√2) ≈ 1.4M × 6.7e-16 ≈ 9.4e-10 per cube per kernel — i.e. zero events expected in 30 s of synthetic cubes (~112 cubes). The validation as literally written is unobservable. Resolution: the bench produces the empirical-vs-analytic curve over θ ∈ {6, 7, 8, 9, 10}; the operator inspects the *shape* of the falloff (it should track the analytic Gaussian tail across the observable range, even if θ=8 itself is sub-1-sample). The {0.5×, 2.0×} bound is then asserted at the lowest θ with ≥10 expected events (typically θ=6 at the bench's grid sizes); plan should pin "lowest θ where N_expected ≥ 10" during hardening. | Implementation derivation; the literal θ=8 check is unobservable at bench-scale cube geometries, but the curve-shape check across {6, 7, 8, 9, 10} captures the same intent. |

---

## Provenance

- **M5 author** (2026-05-05) — initial scaffold from a single-agent reading of plan §8.M2-carryover + §8 M5 + `PARALLEL_AGENTS.md`. No parallel-subagent fan-out yet (M2's pattern was to fan out at the start of each chunk; M5 follows the same pattern as chunks land).
- Items will be added by the chunk-author or by parallel-explore subagents during chunk authoring; M2's pattern was 4 subagents at M2 kickoff producing F1-F13 and 7 more F-items + 5 D-items accumulating across chunks 1-7 + the 2026-05-04/05 user pivots (D14, D17, D18). M5 will follow the same accumulation pattern.

This file is committed at the start of M5 and consumed by the final M5
hardening chunk. It is deleted after the plan is patched and re-locked —
its job is to prevent the F-fixes from being lost between chunks. After
deletion, `tools/dod/M5.sh::status_emit` flips
`plan_fixes_tracker_present` to `false` and rolls the stage label from
`complete (approved)` to `complete (hardened)`.
