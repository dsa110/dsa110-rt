# M2 plan-fix checklist

Captured at the start of M2 from the parallel explore-subagent fan-out
(2026-05-04 21:30Z, transcripts `90319675`, `fd6a8b3b`, `fc5667ab`,
`0a5e56dd`). These are inconsistencies / typos / under-specifications in
`/home/ubuntu/vikram/dev/dsa110-rt_revamp_7b1d2669.plan.md` that the M2
author resolves in code via locked decisions (see "Locked decisions"
below). All plan edits are **deferred to the M2 hardening pass at the end**
(Chunk 8) so the M2 critical path is not blocked by sequential plan-edit
+ scp + relock cycles.

## Plan-fix items (apply during M2 hardening)

| ID | Section | Fix | Source |
|----|---------|-----|--------|
| F1 | §8 line 2168-2170 | Plan DoD describes "per-(ant, pol) cal/bandpass" + "bandpass shape recovered in autos". Legacy `dsaX_bfCorr` correlator mode does NOT apply cal (only `-b` beamformer mode does — `dsaX_bfCorr.cu` line 128-146 only allocates cal storage when `bf==1`; line 1369+ only `fread`s `fcalib` when `bf`). Lock D2 below: M2 fresh-impl does apply cal+bandpass per (ant, ch, pol) before the GEMM, so the legacy non-parity is intentional. Plan should explicitly say "cal applied (not parity)" rather than just listing the test as if it were a parity claim. | Subagent A |
| F2 | (legacy comment) | `dsaX_bfCorr.cu` lines 4-6 top-of-file comment claims "calibrate while reordering" for ALL workflows; correlator mode actually only `corr_input_copy` × 0.05 fluffs without cal. Note in legacy provenance section of plan §3.5 / §0 that this comment is misleading. | Subagent A |
| F3 | §3 / §8 | Plan does not pin output `npol` for slow-corr visibilities. Legacy `dsaX_bfCorr` writes `[NBASE, NCHAN, 2 pol, 2 complex]` — i.e. **2 parallel-pol products** (XX, YY), NOT the 4-hand (XX, XY, YX, YY) form `dsacorr.c` writes. Lock D1 below: M2 emits **2-pol** to match `bada` consumer (`meridian_fringestop` → `dada_to_uvh5` calls `read_buffer(reader, nbls, nchan, npol=2)`; `dsamfs/utils.py` line 141-155). Pin in §3 / §8 + name the 2 products as "0 = XX, 1 = YY" rather than "pol-0, pol-1". | Subagent A + B |
| F4 | §3.5 / §8 | Visibility sign convention is not pinned in plan. `dsacorr.c` (line 187-194) implements `Im ∝ r_a · i_b - i_a · r_b` for ordered pair `(a, b)` with `b ≥ a`, which is `vis = E_a^* E_b` — *not* the textbook `E_a E_b^*` (the conjugate is on the FIRST antenna). `dsamfs/io.py` line 196-197 confirms: "uvw and baseline convention for pyuvdata is B-A, where vis = A^* B". Lock D5 below: `vis_ij = E_i^* E_j` for `(i, j)` with `i ≤ j`. Plan §3.5 should add an explicit "vis sign convention" pin to prevent the inevitable sign-flip bug. | Subagent A + B |
| F5 | §8 line 2165 | Plan says "minimal patch to dsamfs/routines.py" but the **actual** "compute if missing" code lives in `dsamfs/utils.py::load_visibility_model` (line 239-257). M2 author's options: (a) widen the patch to include `utils.py`, OR (b) bypass `load_visibility_model` in `routines.py` and resolve the cache file directly + call `zenith_visibility_model(fobs, path)`. Lock D9 below: option (b) — minimal `routines.py`-only patch + new helper `resolve_nearest_fstable(...)` in `routines.py` (or new `dsamfs/fstable_cache.py`). Plan §8 line 2165 should say "patch `dsamfs/routines.py` (minor: bypass `load_visibility_model`'s online-rebuild path)". | Subagent C |
| F6 | configs/config_corr.yaml (M0 stub) | The repo's current `dsa110-rt/configs/config_corr.yaml` carries STUB buffer sizes that are 5-15× smaller than the legacy `dsa110-cnf/config_dsa96_corr.yaml` (the F7-corrected path): `fada=51,118,080 B (vs legacy 301,989,888)`, `bada=1,996,800 B (vs legacy 28,606,464)`, `dada=94,371,840 B (vs 150,994,944)`, `eada=402,653,184 B (vs 150,994,944)`. The `bada` value in particular CANNOT hold one slow-corr block (`4656 × 384 × 2 × 8 = 28,606,464 B complex64`), so M2's `corr_slow_compute` would fail to write its first block. The repo values were either (a) an early sketch that got mis-checked-in, or (b) M0-smoke remnants — but M0.sh hardcodes its own smoke values (`4 MiB × 4 nbufs`) and does not read this file, so they are not "M0 smoke". M2 Chunk 3 (or a dedicated Chunk 0) corrects this file to mirror legacy. | Subagent B + Chunk-0 verification |
| F7 | §8 line 2164 | Filename precision: legacy `dsamfs/routines.py` line 53-56 writes `fringestopping_table_dec{deg:.1f}deg_…` — `.1f` rounds to 0.1°, so a 0.25° grid would produce duplicates (e.g. `dec0.2` for both 0.20° and 0.25°). Lock D6 below: M2 cache uses `fringestopping_table_dec_{deg:+08.4f}deg_{nant}ant_refmjd{refmjd:.6f}.npz` (4-decimal sign-aware DEC, plus `refmjd` in filename so cache invalidation on `refmjd` drift is a `glob` filter). Plan §8 line 2164 should pin the filename scheme. | Subagent C |
| F8 | bench/replay_voltage_dump.py (current state) + §8 line 2172 | The repo's `bench/replay_voltage_dump.py` only implements `--dry-run`; the non-dry-run path raises `NotImplementedError("M3 owns the PSRDADA writer path")` and exits 2. M2 DoD requires `--rate native` to actually write `fada` blocks. Lock D7 below: M2 owns the full PSRDADA-writer implementation (with the helper `write_one_fada_block(...)` mirroring the Subagent-B-supplied skeleton). Plan §8 line 2172 should say "M2 lands the full `replay_voltage_dump` PSRDADA-writer path". The current code's "M3 owns" comment was speculation. | Subagent D |
| F9 | tools/viz/* + §8 line 2172 | `tools/viz/corr_imager_dedisperser_check.py` and `tools/viz/common.py` do not exist in the repo. M2 must land a minimal implementation with surface `--mode continuum --check slow_corr --uvh5 <path> --voltage-run-id <id> --out <dir>`. Other modes (`--mode burst`, `--chgroup`, etc.) defer to M3+. Plan §8 line 2172 inline-references this CLI but doesn't say "M2 owns the file"; F9 makes it explicit. | Subagent D |
| F10 | §8 line 2119 vs lines 2174-2176 / 1879-1880 | Wording inconsistency: §8 line 2119 says sub-agents "emit `report.html` in PASS state" but lines 2174 / 1879-1880 / 2176 say "no PASS/FAIL banner; operator inspects and signs off". Reconcile §8 line 2119 to match the dominant convention (no PASS/FAIL banner; operator approval is the gate). | Subagent D |
| F11 | §3 line 1823 | Per-fixture-block math: plan says "15 blocks × [2048 pkt, 96 ant, 384 ch, 2 t, 2 pol, 4-bit cplx]" per `fl_*_chgroup<g>.out`. Compute: `2048 × 96 × 384 × 2 × 2 × 1 byte (4-bit cplx packed) = 301,989,888 B/block × 15 = 4,529,848,320 B/file = 4.22 GiB/file × 16 chgroups = 67.5 GiB/fixture` — matches plan's "72 GB / fixture" approximation but not exactly. Tighten the math to "≈ 67.5 GiB / fixture" or "≈ 72 GB / fixture" with explicit units. Minor. | Cross-check |
| F12 | §3 / §3.4 | `bada` output dtype is unstated. Legacy and `meridian_fringestop` consumer expect `complex64` (`dsamfs/utils.py::read_buffer` line 144 does `data.view(np.float32).reshape(-1, 2).view(np.complex64)`). Lock D8 below: M2 emits `complex64` (NOT `cfp16` despite the §3 cfp16 references — those are for the FAST-vis grid, not slow-vis on `bada`). Plan §3 / §3.4 should pin `bada.dtype = complex64` to disambiguate. | Subagent A + B |
| F13 | §8 / §3.3 | No precedent for the operator-approval marker scheme: M0/M1 don't have a "human signed off" gate. Lock D11 below: invent the marker scheme `bench/reports/<UTC>/<run_id>/<milestone>/m<n>_operator_approved.yaml` containing `{operator: <user>, approval_utc: <iso>, milestone: M2, voltage_run_id: <id>, viz_artifact_sha256: <hex>}`; `tools/dod/M2.sh` emits "needs operator approval" and waits for the marker before stamping `stage = complete (approved)`. Add §8 / §7 prose pinning the scheme. | Subagent D |
| F14 | §6.1 / §8 line 2165 | Plan does not pin which conda env runs each M2 piece. h01 inventory: `dsacalib`/`dsamfs`/`antpos`/`casacore` are ONLY in `casa38`; `dsa110-rt` env has `torch`/`psrdada`/`numpy` but none of the cal-pipeline deps. Lock D13 above. Plan §6.1 / §8 should add a "conda env per binary" sub-table: `corr_slow_compute = dsa110-rt`, `replay_voltage_dump = dsa110-rt`, `build_fstable_cache = casa38`, `meridian_fringestop = casa38`. | h01 preflight at d2ade01 |

## Locked decisions (M2 author's resolutions)

These are the values M2 code uses. Plan §3 / §3.4 / §3.5 / §8 will be
updated in Chunk 8 to reflect them where the plan was ambiguous.

| ID | Decision | Locked value | Rationale |
|----|----------|--------------|-----------|
| D1 | Slow-corr output `npol` | 2 (XX = pol_0 × pol_0; YY = pol_1 × pol_1) | Matches legacy `bada` layout + `meridian_fringestop` consumer (`dsamfs/utils.py::read_buffer(reader, nbls, nchan, npol=2)`). The 4-hand form would require a different bada size + a `dada_to_uvh5` rewrite; out-of-scope for M2. |
| D2 | Cal + bandpass applied? | YES — per (ant, ch, pol) complex weight applied to voltages BEFORE the GEMM (so cal is folded into the in-process voltage tensor; the GEMM is unchanged). | Plan DoD test (3) requires "per-(ant, pol) bandpass shape applied to (1) — shape is recovered in the auto-correlations" — this only works if cal+bandpass is in the M2 service. Legacy `dsaX_bfCorr` correlator mode skipped cal because the corr-side cal was applied later in `meridian_fringestop`'s pipeline; the v1 design folds it into corr-slow per plan §4.2 (Stokes-I-on-corr was not specified for slow vis but the cal application IS specified). |
| D3 | GEMM dtype | `torch.matmul` in `complex64` (fp32 real + fp32 imag) | Legacy uses fp16 with `halfFac=4` to dodge fp16 overflow; for M2 fresh impl, fp32 is simpler and "still fits at native cadence" per plan R2. M3+ optimisation can drop to fp16 with the legacy `halfFac` trick if benching shows we need the speed. |
| D4 | GEMM library | `torch.matmul` (cuBLAS Cgemm under the hood) | Mirrors legacy 4× HgemmStridedBatched logic but in a single complex GEMM call per (ch, t, pol) tile. Subagent A's locked-in (i, j) → bls_idx formula gives the upper-triangle scatter. |
| D5 | Visibility sign convention | `V_ij = E_i^* · E_j` for ordered pair `(i, j)` with `i ≤ j` | Confirmed by `dsacorr.c` line 187-194 imag-part formula and `dsamfs/io.py` line 196-197 explicit `vis = A^* B` comment. |
| D6 | Fstable cache filename | `fringestopping_table_dec_{deg:+08.4f}deg_{nant}ant_refmjd{refmjd:.6f}.npz` | 4-decimal-degree precision avoids 0.25°-grid filename collisions; `refmjd` in filename + a `glob` filter at lookup time gives O(1) cache invalidation on `refmjd` drift (vs the legacy `np.load + assert refmjd == X` round-trip). |
| D7 | `replay_voltage_dump` PSRDADA writer | M2 lands it (not M3) | The plan §8 M2 DoD requires `--rate native`. Implementing it in M3 was a bench-author-side comment, not a plan decision. |
| D8 | `bada` block dtype | `complex64` (=`cfp32`); shape `(nbls=4656, nchan=384, npol=2)` per integration | Matches legacy + consumer expectation; `28,606,464 B = 4656 × 384 × 2 × 8`. |
| D9 | `dsamfs/routines.py` patch shape | Bypass `load_visibility_model` entirely; new local helper `_resolve_nearest_fstable_cache(cache_dir, pt_dec_rad, refmjd, nant) → (dec_rad, fs_table_path)` in `routines.py`. No edits to `dsamfs/utils.py`. | Plan literally says "patch `routines.py`"; honour that. The `utils.load_visibility_model` retains its online-rebuild semantics for non-realtime users. |
| D10 | UVH5 `phase_center_app_dec` value | The **nearest cached DEC** (the one the cache file was built for), NOT the live etcd `pt_dec`. UVW continues to derive from live `pt_dec` per legacy. | Plan §8 line 2171 ("nearest cached DEC"). The slight DEC mismatch between UVW (live pt_dec) and `phase_center_app_dec` (nearest cached) is at most 0.125° = grid step / 2; cal pipeline tolerates this. |
| D11 | Operator-approval marker scheme | `bench/reports/<UTC>/<run_id>/<M_label>/m_operator_approved.yaml` with fields `{operator, approval_utc_iso, milestone, voltage_run_id, viz_artifact_sha256}`. The presence of this file is what `tools/dod/M2.sh::operator_approval` checks. | Reuses M0/M1 status-JSON convention pattern; sidecar yaml in the same report dir is the natural place. |
| D12 | Cal source for synth tests | Inline-built per-test (no etcd / no cache files); deterministic via `np.random.default_rng(seed=...)` | Matches plan §8 line 2167 "deterministic synthetic inputs with `np.random.default_rng(seed=...)`". |
| D13 | Conda-env split for M2 | **`dsa110-rt`** runs: `services/corr_slow_compute.py`, `bench/replay_voltage_dump.py`, `tests/test_slow_corr_synth.py`, `tools/dod/M2*.sh`, `tools/viz/corr_imager_dedisperser_check.py`, `bench/voltage_fixture_slow_corr.py`. **`casa38`** runs: `tools/build_fstable_cache.py` (calls `dsamfs.fringestopping.generate_fringestopping_table`), the patched `dsamfs/routines.py` (loaded by `meridian_fringestop`), and `meridian_fringestop` itself. M2.sh orchestrates: corr_slow + replay + viz in `dsa110-rt`; calls `build_fstable_cache` and `meridian_fringestop` via explicit casa38-env wrappers. | h01 inventory shows `dsacalib`, `dsamfs`, `antpos`, `casacore` only installed in `casa38` env — no clean path to dual-install without major env work. The legacy `meridian_fringestop` already runs in `casa38`; M2 inherits the env split from the legacy reality. F14 below tracks the plan-prose update. |

## Provenance

- **Subagent A** — Legacy slow-corr code survey (id `90319675`, 2026-05-04 21:30Z) — pinned `dsaX_bfCorr.cu` corr-mode as canonical; produced (i, j) → bls_idx formula; flagged cal-not-applied legacy gotcha + 2-vs-4-hand pol gotcha.
- **Subagent B** — `psrdada-python` Reader/Writer + fada/bada layout survey (id `fd6a8b3b`, 2026-05-04 21:30Z) — pinned API; flagged config_corr.yaml buffer-size mismatch (F6); supplied fada-read + bada-write Python skeleton.
- **Subagent C** — `dsamfs/routines.py` + fstable cache survey (id `fc5667ab`, 2026-05-04 21:30Z) — pinned `(nint, n_bl)` `bw` table layout; flagged compute-if-missing-actually-in-utils.py (F5) + filename precision (F7).
- **Subagent D** — Voltage-fixture replay + viz survey (id `0a5e56dd`, 2026-05-04 21:30Z) — confirmed F8 (replay PSRDADA writer not implemented), F9 (no viz tool), F13 (no operator-approval scheme).
- **Cross-checks** — F11 (per-fixture math), F12 (bada dtype) from layered subagent reports.

This file is committed at the start of M2 and consumed by Chunk 8 (hardening). It is
deleted after the plan is patched and re-locked — its job is to prevent the F1-F13
fixes from being lost between chunks.
