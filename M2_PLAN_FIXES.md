# M2 plan-fix checklist

Captured at the start of M2 from the parallel explore-subagent fan-out
(2026-05-04 21:30Z, transcripts `90319675`, `fd6a8b3b`, `fc5667ab`,
`0a5e56dd`). These are inconsistencies / typos / under-specifications in
`/home/ubuntu/vikram/dev/dsa110-rt_revamp_7b1d2669.plan.md` that the M2
author resolves in code via locked decisions (see "Locked decisions"
below). All plan edits are **deferred to the M2 hardening pass at the end**
(Chunk 8) so the M2 critical path is not blocked by sequential plan-edit
+ scp + relock cycles.

## Plan-fix items (apply during M2 hardening)

| ID | Section | Fix | Source |
|----|---------|-----|--------|
| F1 | §8 line 2168-2170 | Plan DoD prose for test (3) reads "per-(ant, pol) bandpass shape applied to (1)" ambiguously. Per D2 above (revised 2026-05-04 by user pivot): cal is NEVER applied in `corr_slow_compute`. The DoD test is reinterpreted as: synthetic input voltages carry a non-flat per-(ant, pol) passband; corr autos recover that passband shape `\|H(ν)\|^2 × σ_th^2`. The corr does NOT apply cal. Plan §8 should say "synthetic per-(ant, pol) bandpass applied to the INPUT VOLTAGES; auto-correlations recover the bandpass shape (GEMM-faithfulness check, not cal-application)". | Subagent A + user pivot 2026-05-04 |
| F2 | (legacy comment) | `dsaX_bfCorr.cu` lines 4-6 top-of-file comment claims "calibrate while reordering" for ALL workflows; correlator mode actually only `corr_input_copy` × 0.05 fluffs without cal. Note in legacy provenance section of plan §3.5 / §0 that this comment is misleading. | Subagent A |
| F3 | §3 / §8 | Plan does not pin output `npol` for slow-corr visibilities. Legacy `dsaX_bfCorr` writes `[NBASE, NCHAN, 2 pol, 2 complex]` — i.e. **2 parallel-pol products** (XX, YY), NOT the 4-hand (XX, XY, YX, YY) form `dsacorr.c` writes. Lock D1 below: M2 emits **2-pol** to match `bada` consumer (`meridian_fringestop` → `dada_to_uvh5` calls `read_buffer(reader, nbls, nchan, npol=2)`; `dsamfs/utils.py` line 141-155). Pin in §3 / §8 + name the 2 products as "0 = XX, 1 = YY" rather than "pol-0, pol-1". | Subagent A + B |
| F4 | §3 line 821 (REVISED 2026-05-04 by Chunk-4 sign-convention test) | Plan §3 line 821 IS pinned, but **pinned wrong**. The line declares `"V_ij = E_i × conj(E_j), NOT conj(E_i) × E_j"` then cites bfCorr's GEMM (`real×real, imag×imag, −imag×real, real×imag`) and gives formulas: `V_ij.real = R_i R_j + I_i I_j`, `V_ij.imag = I_i R_j − R_i I_j`. The cited formulas **actually describe `conj(E_i) × E_j`**, not `E_i × conj(E_j)` — the prose claim and the formulas are inconsistent. bfCorr's actual behaviour, `dsacorr.c` line 187-194, and `dsamfs/io.py` line 196-197 all produce `V_ij = E_i^* · E_j` (conjugate on the FIRST antenna). D5 below pins the correct convention. The Chunk-4 test `test_kernel_sign_convention_v_ij_conj_ei_ej` constructs E_a = (1, 0), E_b = (0, 1) and asserts `V[a, b].imag = +K · scale²` — which would fail with the plan's incorrect prose claim. Plan §3 line 821 should: (a) drop the `"NOT conj(E_i) × E_j"` clause; (b) replace `E_i × conj(E_j)` with `conj(E_i) × E_j`; (c) keep the GEMM formulas as-is (they are correct for the real convention). | Subagent A + B + Chunk-4 test verification 2026-05-04 |
| F5 | §8 line 2165 | **OBSOLETE per user pivot 2026-05-04 (D14)**. The plan's "patch dsamfs/routines.py" sub-deliverable is dropped from M2. Plan §8 line 2165 should be removed or reduced to "M2 builds a per-DEC fringestop-table cache; how `meridian_fringestop` consumes the cache is operator-side". | Subagent C + user pivot 2026-05-04 |
| F6 | configs/config_corr.yaml (M0 stub) | The repo's current `dsa110-rt/configs/config_corr.yaml` carries STUB buffer sizes that are 5-15× smaller than the **physics-pinned** values: `fada=51,118,080 B (vs 301,989,888 = 2048×96×384×2t×2p×1byte)`, `bada=1,996,800 B (vs 28,606,464 = 4656×384×2×8byte)`, `dada=94,371,840 B`, `eada=402,653,184 B`. The repo's stubs match `dsa110-xengine/scripts/config_corr.yaml` on h01 (a smaller-scale dev variant); the canonical 96-ant production sizes are physics-pinned and live as a working copy `dsa110-cnf/config_dsa96_corr.yaml` on h23 (uncommitted on the development branch — `git status` shows it as `??`). The `bada` value in particular CANNOT hold one slow-corr block, so M2's `corr_slow_compute` would fail to write its first block. M2 Chunk 0 corrects `dsa110-rt/configs/config_corr.yaml` to the physics-pinned values; the legacy yaml file stays out of the M2 preflight (it's not on h01 and the values are physics-pinned, not config-pinned). The plan's F7-corrected pointer to `dsa110-cnf/config_dsa96_corr.yaml` becomes a "legacy provenance pointer" only. | Subagent B + h01 inventory at d2ade01 |
| F7 | §8 line 2164 | Filename precision: legacy `dsamfs/routines.py` line 53-56 writes `fringestopping_table_dec{deg:.1f}deg_…` — `.1f` rounds to 0.1°, so a 0.25° grid would produce duplicates (e.g. `dec0.2` for both 0.20° and 0.25°). Lock D6 below: M2 cache uses `fringestopping_table_dec_{deg:+08.4f}deg_{nant}ant_refmjd{refmjd:.6f}.npz` (4-decimal sign-aware DEC, plus `refmjd` in filename so cache invalidation on `refmjd` drift is a `glob` filter). Plan §8 line 2164 should pin the filename scheme. | Subagent C |
| F8 | bench/replay_voltage_dump.py (current state) + §8 line 2172 | The repo's `bench/replay_voltage_dump.py` only implements `--dry-run`; the non-dry-run path raises `NotImplementedError("M3 owns the PSRDADA writer path")` and exits 2. M2 DoD requires `--rate native` to actually write `fada` blocks. Lock D7 below: M2 owns the full PSRDADA-writer implementation (with the helper `write_one_fada_block(...)` mirroring the Subagent-B-supplied skeleton). Plan §8 line 2172 should say "M2 lands the full `replay_voltage_dump` PSRDADA-writer path". The current code's "M3 owns" comment was speculation. | Subagent D |
| F9 | tools/viz/* + §8 line 2172 | `tools/viz/corr_imager_dedisperser_check.py` and `tools/viz/common.py` do not exist in the repo. M2 must land a minimal implementation with surface `--mode continuum --check slow_corr --uvh5 <path> --voltage-run-id <id> --out <dir>`. Other modes (`--mode burst`, `--chgroup`, etc.) defer to M3+. Plan §8 line 2172 inline-references this CLI but doesn't say "M2 owns the file"; F9 makes it explicit. | Subagent D |
| F10 | §8 line 2119 vs lines 2174-2176 / 1879-1880 | Wording inconsistency: §8 line 2119 says sub-agents "emit `report.html` in PASS state" but lines 2174 / 1879-1880 / 2176 say "no PASS/FAIL banner; operator inspects and signs off". Reconcile §8 line 2119 to match the dominant convention (no PASS/FAIL banner; operator approval is the gate). | Subagent D |
| F11 | §3 line 1823 | Per-fixture-block math: plan says "15 blocks × [2048 pkt, 96 ant, 384 ch, 2 t, 2 pol, 4-bit cplx]" per `fl_*_chgroup<g>.out`. Compute: `2048 × 96 × 384 × 2 × 2 × 1 byte (4-bit cplx packed) = 301,989,888 B/block × 15 = 4,529,848,320 B/file = 4.22 GiB/file × 16 chgroups = 67.5 GiB/fixture` — matches plan's "72 GB / fixture" approximation but not exactly. Tighten the math to "≈ 67.5 GiB / fixture" or "≈ 72 GB / fixture" with explicit units. Minor. | Cross-check |
| F12 | §3 / §3.4 | `bada` output dtype is unstated. Legacy and `meridian_fringestop` consumer expect `complex64` (`dsamfs/utils.py::read_buffer` line 144 does `data.view(np.float32).reshape(-1, 2).view(np.complex64)`). Lock D8 below: M2 emits `complex64` (NOT `cfp16` despite the §3 cfp16 references — those are for the FAST-vis grid, not slow-vis on `bada`). Plan §3 / §3.4 should pin `bada.dtype = complex64` to disambiguate. | Subagent A + B |
| F13 | §8 / §3.3 | No precedent for the operator-approval marker scheme: M0/M1 don't have a "human signed off" gate. Lock D11 below: invent the marker scheme `bench/reports/<UTC>/<run_id>/<milestone>/m<n>_operator_approved.yaml` containing `{operator: <user>, approval_utc: <iso>, milestone: M2, voltage_run_id: <id>, viz_artifact_sha256: <hex>}`; `tools/dod/M2.sh` emits "needs operator approval" and waits for the marker before stamping `stage = complete (approved)`. Add §8 / §7 prose pinning the scheme. | Subagent D |
| F14 | §6.1 / §8 line 2165 | Plan does not pin which conda env runs each M2 piece. h01 inventory: `dsacalib`/`dsamfs`/`antpos`/`casacore` are ONLY in `casa38`; `dsa110-rt` env has `torch`/`psrdada`/`numpy` but none of the cal-pipeline deps. Lock D13 above. Plan §6.1 / §8 should add a "conda env per binary" sub-table: `corr_slow_compute = dsa110-rt`, `replay_voltage_dump = dsa110-rt`, `build_fstable_cache = casa38`, `meridian_fringestop = casa38 (UNTOUCHED legacy)`. | h01 preflight at d2ade01 + user pivot 2026-05-04 |
| F15 | §8 line 2168-2170 cal lines, §4.2 | Plan should explicitly state: M2 `corr_slow_compute` does NOT apply cal/bandpass. Cal application is M3+ only (fast-vis path). Slow visibilities are USED FOR cal solving (operator-side / out-of-scope) but not modified by the corr service. Plan §4.2 currently implies cal could land at corr-side; clarify slow-vs-fast asymmetry. | User pivot 2026-05-04 |
| F16 | §8 line 2171 | "meridian_fringestop reads the produced `bada` and writes a UVH5 file with `phase_center_app_dec` = nearest cached DEC" — drop the "= nearest cached DEC" qualifier per D14. The DoD line becomes: "meridian_fringestop reads the produced `bada` and writes a UVH5 file (legacy DEC handling); the existing dsa110-calib pipeline returns sane gains on this UVH5". | User pivot 2026-05-04 |
| F17 | §8 line 2172, §4.2 | Plan §8 DoD references "image a continuum source" but does not say where cal application happens for that imager check. Lock D17 below: `corr_slow_compute --apply-cal <path>` is a TEST-ONLY flag (default OFF). Plan §8 should add a sub-bullet under DoD test (5): "for the imager check, `bench/voltage_fixture_slow_corr.py` invokes `corr_slow_compute --apply-cal` to produce the calibrated bada that `corr_imager_dedisperser_check.py --mode continuum` images". Plan §4.2 should clarify "production never sets `--apply-cal`; cal application in production is downstream of slow-corr in user-side code". | User pivot 2026-05-05 (Option B chosen for cal-app architecture) |
| F18 | §3 line 821 + slow_corr_kernel.py upper-tri gather (NEW 2026-05-05) | **Sign-convention bug found 2026-05-05** during D17 imaging acceptance. The user imaged the un-fringestopped UVH5 (with their own `uvh5_to_ms_0319.py` MS converter and CASA `tclean`) and reported the field "exactly mirrored in x and y about the zenith position" — a textbook conjugate-baselines symptom. Root cause: PyTorch's `torch.matmul(R^T, R)` writes the result ROW-MAJOR into a `(NANTS, NANTS)` tile, so `V[i, j]` literally equals `sum_t conj(E_i) · E_j`. Our upper-tri gather originally extracted at `(_a_idx, _b_idx) = (higher, lower)` — `V[a, b] = conj(E_high) · E_low`. But `dsamfs/io.py:196-197` writes UVH5 with `vis = A^* B` where `A = ant_1 = lower` index and `B = ant_2 = higher` index — i.e. `conj(E_low) · E_high`. Our output was therefore the **complex conjugate** of dsamfs/CASA convention. bfCorr (`dsaX_bfCorr.cu` lines 559-581) gets the right answer despite using the same per-element formula because `cublasHgemm` writes COLUMN-MAJOR while the post-GEMM gather (`h_idxs[k] = i*NANTS + j`) reads ROW-MAJOR — that implicit transpose is exactly what we need. PyTorch has no such implicit transpose, so we have to swap the gather indices explicitly. **Fix**: `slow_corr_kernel.py` Stage-4 gather changed from `V_real_b[..., self._a_idx, self._b_idx]` to `V_real_b[..., self._b_idx, self._a_idx]` (V_real is symmetric in (a, b), V_imag is antisymmetric, so this flips the imag sign without recomputing — equivalent to `vis_imag *= -1`). Test `test_kernel_sign_convention_v_ant1_conj_ant2` (was `_v_ij_conj_ei_ej`) updated to assert `vis_imag = +K · scale²` (positive, matching dsamfs convention). Test `test_point_source_planar_wave_phase_recovery` analytic formula updated from `np.conj(E[a_idx]) * E[b_idx]` (high, low) to `np.conj(E[b_idx]) * E[a_idx]` (low, high). Plan §3 line 821 prose stays correct (`V_ij = conj(E_i) · E_j`) but should add a parenthetical "(implementation note: PyTorch matmul writes row-major; bfCorr cuBLAS writes column-major; same per-element formula but the upper-tri gather indices differ — see slow_corr_kernel.py Stage 4)". | User CASA-imaging finding 2026-05-05 + dsamfs/io.py audit. Re-verified end-to-end via 0319+415 zenith-imaging acceptance on h01. |
| F19 | §8 line 2171-2172 (M2 imaging DoD scoping; NEW 2026-05-05) | **Imaging-acceptance scope clarified 2026-05-05**: M2 is responsible for verifying the slow-corr + cal-application path, NOT the `meridian_fringestop` fringestop logic (which has its own dedicated tests in legacy code). Per user request 2026-05-05, the M2 imaging acceptance now runs `meridian_fringestop` with **`fringestop: false`** (drift / zenith-phased visibilities) and overrides the wrapper's pt_dec to **`PHI_LAT_OVRO_DEG = 37.234`** so dsamfs's `baseline_uvw` writes UVW in the **zenith frame** (matching the un-fringestopped visibility phases). The imager (`corr_imager_dedisperser_check.py --mode continuum`) sees a self-consistent (vis-phase, uvw-frame) pair and recovers the continuum source at the predicted (l, m) from zenith. `bench/run_0319_pipeline.py --no-fringestop` now defaults `pt_dec_deg → PHI_LAT_OVRO_DEG` automatically, computes the expected (l, m) from astropy LST(refmjd) + the phase-center mode, and writes it into the manifest's `continuum_sources[0].lm_rad` so the imager overlays the prediction. Plan §8 lines 2171-2172 should be revised to explicitly pin the zenith-imaging acceptance and reference F19 for the rationale (decoupling M2 cal-application validation from meridian_fringestop's fringestop validation). | User request 2026-05-05 |
| F20 | tools/viz/common.py:grid_uv_natural (NEW 2026-05-05; sign-fix complement to F18) | **Internal-imager (l, m) axis label fix 2026-05-05.** While end-to-end-validating F18 on h01 against `dsacalib.uvh5_to_ms` + CASA `tclean`, the post-F18 UVH5 was confirmed CORRECT in CASA: peak at `(RA, Dec) = (49.944°, 41.514°)` matches the 0319+415 truth `(49.951°, 41.512°)` to within 0.007° (SNR=40.4 in a 10°-FOV 1200×1200 dirty image). Pre-F18 the CASA peak was at `(47.230°, 32.740°)` — exactly mirrored about the phase center on BOTH RA and Dec axes, matching the user's original "exactly mirrored in x and y about the zenith position" report (h23 imaging 2026-05-05). **F18 is therefore correct.** However, our internal `tools/viz/corr_imager_dedisperser_check.py` (which uses `np.fft.ifft2` on `uvw_array`-as-stored) reports the peak at `(l = -0.0188, m = -0.0764)` rad — the same sky position CASA shows at the predicted `(+0.013, +0.075)` rad, but with axis labels FLIPPED. Empirical bisection (`scratch/check_imager_uvw_negate.py`): negating `uvw[:, 0:2]` before gridding restores TMS `(l, m)` axis labels — peak now at `(+0.0188, +0.0764)` rad ≈ predicted, SNR=32.4 pol-0 / 31.8 pol-1. Root cause is that DSA-110's voltage time-frequency convention gives `V(u, v) ∝ exp(+2πi · b · ŝ · ν/c)` (opposite sign of textbook CASA `V = ∫ I exp(-2πi(ul+vm))`). CASA's `dsacalib.uvh5_to_ms.phase_visibilities` + pyuvdata's `conjugate_bls("ant2<ant1")` MS-write step internally flip uvw + conjugate vis to land in CASA's convention, which is why CASA produces correct images out of the post-F18 UVH5. Our internal imager doesn't do that flip, so we negate `(u, v)` in the gridder to compensate. **Fix**: `tools/viz/common.py::grid_uv_natural` now multiplies `uvw_m[:, 0]` and `uvw_m[:, 1]` by `-1` before gridding, with a comment pointing here. Verified end-to-end on h01 against the post-F18 + `--no-fringestop` UVH5s: `M2_continuum_postF18_F20fix/zenith_pol{0,1}/` reports show source at predicted `(l, m)` for both pols. | h01 CASA `tclean` ground truth on `0319_uvh5_zenith_preF18` + `0319_uvh5_zenith` 2026-05-05 (`scratch/casa_F18_test/casa_image_one.py`). |

## Locked decisions (M2 author's resolutions)

These are the values M2 code uses. Plan §3 / §3.4 / §3.5 / §8 will be
updated in Chunk 8 to reflect them where the plan was ambiguous.

| ID | Decision | Locked value | Rationale |
|----|----------|--------------|-----------|
| D1 | Slow-corr output `npol` | 2 (XX = pol_0 × pol_0; YY = pol_1 × pol_1) | Matches legacy `bada` layout + `meridian_fringestop` consumer (`dsamfs/utils.py::read_buffer(reader, nbls, nchan, npol=2)`). The 4-hand form would require a different bada size + a `dada_to_uvh5` rewrite; out-of-scope for M2. |
| D2 | Cal + bandpass applied in slow-corr? | **NO in production, OPTIONAL in tests — user-provided constraint (2026-05-04, refined 2026-05-05)**. `corr_slow_compute` outputs UNCALIBRATED visibilities by default; in production the `--apply-cal` flag is never set and the kernel runs identically to the cal-free path. For test-only use (M2 DoD imager check, Chunk 6), the same binary accepts `--apply-cal <cal_table_path>` which loads cal solutions and applies them post-GEMM, before the bada write. Cal-application code stays dormant in production but lives in the same binary so the test path exercises the SAME I/O / threading / lifecycle as production (mirrors a single source of truth for the slow-corr GEMM). The user derives cal solutions FROM uncalibrated slow visibilities downstream in their own code; that path is unchanged. The plan §8 DoD test (3) "per-(ant, pol) bandpass shape applied to (1) — shape is recovered in the auto-correlations" is reinterpreted: bandpass is applied to the SYNTHETIC INPUT VOLTAGES at test time (modeling a real per-(ant, pol) passband), and the test verifies the corr's auto-correlations faithfully reproduce `\|bandpass(ν)\|^2 × thermal_noise(ν)` — i.e. GEMM faithfulness through a non-flat bandpass, NOT cal application by the corr. | User pivots 2026-05-04 (no-cal in production) + 2026-05-05 (Option B: in-binary opt-in flag for testing). Matches legacy `dsaX_bfCorr` correlator-mode behaviour exactly when `--apply-cal` is unset (`dsaX_bfCorr.cu` line 128-146). |
| D3 | GEMM dtype | **REVISED 2026-05-04 (user pivot)**: fp16 inputs, fp16 chained accumulators (mirroring bfCorr's `beta=0.0` then `beta=1.0`), single fp32 cast after summing over 2 t_sub samples. Real-imag split form: V_real = R^T@R + I^T@I, V_imag = R^T@I - I^T@R → 4 real fp16 batched matmuls per block. Tensor cores (HMMA on Turing 2080 Ti) ENGAGE only with fp16 inputs (TF32 is Ampere+); fp32 inputs give 13 TFLOPs vs 130 TFLOPs at fp16-with-fp32-accum. fp32 was empirically 786 ms / block (>5× over the 134 ms native budget) AND OOM'd at 11 GB VRAM. | Chunk 3 first-impl benchmark on h01 (e01db1e), user pivot 2026-05-04 ("tensor cores are essential"), and bfCorr re-read (user hint "have you checked how bfCorr does it?"). Final compute time on 2080 Ti: 8.9 ms / block (~26 TFLOPs effective; ~20% peak HMMA, normal for M=N=96 small-matrix overhead). |
| D4 | GEMM library | **REVISED 2026-05-04**: `torch.matmul(fp16_input, fp16_input.T)` with batching mirroring bfCorr exactly: batch = NCHAN * 2t * NPOL * half_fac (= 1536 at default half_fac=1), M = N = NANTS = 96, K = NPACKETS_PER_BLOCK / half_fac (= 2048 at default). 4 hgemms chained via in-place `.add_` / `.sub_` on shared fp16 V_real / V_imag buffers. Sum over (t_sub, half_fac) and cast to fp32 happen once at the end. Upper-triangle gather (D5) unchanged. | Same as D3. PyTorch's `torch.matmul` engages HMMA on fp16 inputs (verified via diag harness: 26 TFLOPs at fp16 vs 6 TFLOPs at fp32, 4.3× speedup). |
| D15 | Voltage layout transformation (NEW 2026-05-04) | bfCorr's `transpose_matrix_char` does a 2D byte transpose with shared-memory tiles in ~1 ms; PyTorch's byte transpose is unfortunately a generic strided-copy fallback (~92 ms for 302 MB). Replaced with a fp32-reinterpret trick: view the 4-byte (2t, 2p) cubes as fp32 elements, do PyTorch's optimised fp32 2D transpose (~24 ms), follow with a small inner-axis byte permute (~2 ms) to land in bfCorr's GEMM layout `(NCHAN, 2t, 2p, NPACKETS, NANTS)`. Net: ~26 ms layout transformation (vs 92 ms native byte transpose) and total per-block budget 120 ms (vs 134 ms native cadence) on a 2080 Ti. | Chunk 3 perf bench post-`f082320`. The fp32 reinterpret works because the (2t, 2p) byte pair is contiguous in fada layout (strides 2 and 1) — a coincidence of the fada packed format that lets us tile-transpose 4 bytes at a time without a custom kernel. |
| D16 | Fluff math (NEW 2026-05-04) | bfCorr `corr_input_copy` (lines 281-284) does `((char)byte << 4) >> 4` for the real nibble and `(char)byte >> 4` for the imag nibble in a single fused CUDA kernel (~1 ms). PyTorch can replicate this exactly: reinterpret uint8 as int8 (free, same memory), apply `<<` and `>>` on int8 (PyTorch's int8 right-shift IS arithmetic / sign-preserving on both CPU and CUDA, verified empirically). Two int8 shifts + two int8→fp16 casts × 0.05 = ~13 ms total. The earlier LUT-gather approach was slower because it required `.to(torch.long)` which made a 2.3 GB int64 idx temp from a 288 MB input. | Chunk 3 perf bench post-`de38e64`. |
| D17 | Cal application architecture (NEW 2026-05-05) | `corr_slow_compute --apply-cal <cal_table_path>` is a test-only flag, default OFF. When set, cal is applied PRE-GEMM (per-(ant, ch, pol), not per-baseline post-GEMM): `voltages_cal = G · voltages` then standard GEMM produces `V_cal_ij = G_i^* G_j V_raw_ij`. Cal-table is the legacy `beamformer_weights_*.dat` blob (74,496 bytes, fp32 LE: 96 antpos_e + 96 antpos_n + 96×48×2×2 cal[ant, ch_coarse, pol, ri]; 48 coarse channels each cover 8 fine voltage channels per `bfCorr.cu:1109`). Loader: `dsart.cal.bf_weights.load_bf_weights`. Two `--cal-mode` modes: `phase` (default; `wnorm`-style \|G\|=1, fp16 path → tensor cores) and `full` (preserve amplitudes, fp32 path → no tensor cores; required because real cals can have \|G\|>5e3 → squared visibilities overflow fp16 max 6.5e4). The flag is exercised by `bench/voltage_fixture_slow_corr.py` for the M2 imager DoD only; it is NEVER set in production systemd unit files / runbooks. **Implementation 2026-05-05 (verified end-to-end on h01)**: `src/dsart/cal/bf_weights.py` (loader + helpers `upsample_coarse_to_fine`, `normalize_phase_only`, `maybe_swap_pol`); `src/dsart/services/slow_corr_kernel.py::apply_cal_split` (per-(ant, ch, pol) complex multiply, dtype-generic for fp16/fp32 paths); `src/dsart/services/corr_slow_compute.py` (`--apply-cal` / `--cal-mode` / `--cal-pol-swap` CLI + dtype routing); `bench/casa38_meridian_wrapper.py` (3 monkey-patches: `get_pointing_declination` override, `put_outrigger_delays`/`put_refmjd` no-op, AND `read_buffer` with hard-cap+isEndOfData+empty-page → SystemExit to dodge a latent dsamfs/io.py:343 reshape bug after the first integration); `bench/run_0319_pipeline.py` (per-sb fixture + dsamfs param yaml + dispatch driver). 16/16 cal unit tests passing on h23 `py310t3`; M2 voltage-fixture imaging DoD proven against the 0319+415 dataset (15 sbs × 96 ants × 48 freq × 2 pol UVH5 emitted, all rc=0, 0 NaN/inf). corr_slow_compute also marks bada EOD post-loop so the dsamfs reader sees a clean stream end. **Imaging acceptance 2026-05-05**: `tools/concat_uvh5.py` (pyuvdata `fast_concat(axis="freq")`, runs in casa38 because pyuvdata 3.x rejects float32 uvw_array) merges the 15 per-sb UVH5s into `0319_all.uvh5` (Nblts=4656 × Nfreqs=720 × Npols=2). `tools/viz/corr_imager_dedisperser_check.py --uvh5 0319_all.uvh5 --fov-rad 0.05 --n-grid 2048` images the full multi-spw cube (and `--uvh5-glob` does the same in-tool with the per-sb files via the new `load_uvh5_concat`). Source 0319+415 detected at SNR≈10 in single-sb / SNR≈14 in multi-sb concat in BOTH pol-0 (XX) and pol-1 (YY); peak position (l=+0.019, m=+0.011) rad agrees within ~5 % with the predicted residual fringe `l = sin(HA_src - HA_ref) ≈ +0.0179 rad` for the supplied refmjd 61108.998673 (HA_src = -1.02° from astropy + OVRO siting). The dirty image's vertical / horizontal stripes are PSF sidelobes characteristic of a single-time-integration snapshot with the array's non-redundant uv coverage (2D coverage confirmed: u-std 1860 λ, v-std 3040 λ; `uv_coverage_sb00.png`). Reports written to `/home/ubuntu/data/vikram/0319_reports/M2_continuum/{single_sb00,all_concat_pol0,all_concat_pol1,all_concat_hires,all_concat_widefov,per_sb_concat}/`. `load_uvh5` was extended to accept both pyuvdata-1.x's 4-D visdata `(Nblts, Nspws=1, Nfreqs, Npols)` and the 3-D 3.x layout. **NOTE 2026-05-05 (post-F18 sign fix)**: those (l,m) numbers are from the PRE-fix run (mirror-about-zenith bug). Post-F18 + F19 + F20, the imaging acceptance is RE-RUN and PASSING on h01 with `--no-fringestop` + zenith uvw: post-F18 CASA `tclean` peak at (RA=49.944°, Dec=41.514°) matches 0319+415 truth (49.951°, 41.512°) to 0.007° at SNR=40.4 (10° FOV, 1200×1200 imsize); internal `corr_imager_dedisperser_check.py --mode continuum` peak at (l=+0.0188, m=+0.0764) rad ≈ predicted (+0.0134, +0.0747) at SNR=32.4 pol-0 / 31.8 pol-1. | User pivot 2026-05-05: Option B from the cal-arch question. Single-binary path keeps test and production sharing the same I/O / threading / lifecycle code. |
| D18 | M2 imaging-acceptance phase-center mode (NEW 2026-05-05) | `bench/run_0319_pipeline.py --no-fringestop` (the M2 imaging acceptance default per F19) sets `meridian_param.fringestop = False` AND auto-defaults `--meridian-pt-dec-deg` to `PHI_LAT_OVRO_DEG = 37.234` so dsamfs's `baseline_uvw` writes UVW in the **OVRO zenith frame**. The orchestrator computes the expected source (l, m) from `(src_ra, src_dec, refmjd)` using astropy's LST at the OVRO `EarthLocation(lat=37.234°, lon=-118.281°, height=1222 m)`, applying the standard direction-cosine formulas (Thompson, Moran, Swenson Eq. 3.83): `l = -cos(δ_src) sin(HA_src)`, `m = sin(δ_src) cos(δ_pc) - cos(δ_src) sin(δ_pc) cos(HA_src)` with `δ_pc = PHI_LAT_OVRO_DEG`. The result is written into the manifest's `continuum_sources[0].lm_rad` (already supported by the schema) so the imager overlays the predicted spot without any new CLI argument. `phase_center_mode`, `phase_center_dec_deg`, `ha_src_deg_at_refmjd` are also written into the manifest for traceability. For the legacy `fringestop=True` mode (still exercised when `--no-fringestop` is OFF), `phase_center_dec_deg = src_dec` and the formulas degenerate to `l ≈ -cos(δ_src) sin(HA_src)`, `m ≈ sin(2δ_src)/2 · (1 - cos(HA_src))` — both ~10⁻³ rad for HA_src ~1°. Either way the imager sees a consistent (vis-phase, uvw-frame, expected-overlay) triple. Verified for 0319+415 (RA=49.9507°, Dec=41.5117°, refmjd=61108.99867): predicted zenith-frame `(l, m) ≈ (+0.0133, +0.0738) rad` (≈ 0.76°, 4.23° NE of zenith). | User request 2026-05-05 ("don't test meridian_fringestop's fringestopping; just image the no-fringestop UVH5 assuming zenith"). |
| D5 | Visibility sign convention | `V_ij = E_i^* · E_j` for ordered pair `(i, j)` with `i ≤ j` (i.e. `vis = E_ant_1^* · E_ant_2`, ant_1 < ant_2 — see F18 for the row-major-vs-column-major caveat that bit us in PyTorch matmul) | Confirmed by `dsacorr.c` line 187-194 imag-part formula and `dsamfs/io.py` line 196-197 explicit `vis = A^* B` comment. |
| D6 | Fstable cache filename | `fringestopping_table_dec_{deg:+08.4f}deg_{nant}ant_refmjd{refmjd:.6f}.npz` | 4-decimal-degree precision avoids 0.25°-grid filename collisions; `refmjd` in filename + a `glob` filter at lookup time gives O(1) cache invalidation on `refmjd` drift (vs the legacy `np.load + assert refmjd == X` round-trip). |
| D7 | `replay_voltage_dump` PSRDADA writer | M2 lands it (not M3) | The plan §8 M2 DoD requires `--rate native`. Implementing it in M3 was a bench-author-side comment, not a plan decision. |
| D8 | `bada` block dtype | `complex64` (=`cfp32`); shape `(nbls=4656, nchan=384, npol=2)` per integration | Matches legacy + consumer expectation; `28,606,464 B = 4656 × 384 × 2 × 8`. |
| D9 | `dsamfs/routines.py` patch shape | Bypass `load_visibility_model` entirely; new local helper `_resolve_nearest_fstable_cache(cache_dir, pt_dec_rad, refmjd, nant) → (dec_rad, fs_table_path)` in `routines.py`. No edits to `dsamfs/utils.py`. | Plan literally says "patch `routines.py`"; honour that. The `utils.load_visibility_model` retains its online-rebuild semantics for non-realtime users. |
| D10 | UVH5 `phase_center_app_dec` value | The **nearest cached DEC** (the one the cache file was built for), NOT the live etcd `pt_dec`. UVW continues to derive from live `pt_dec` per legacy. | Plan §8 line 2171 ("nearest cached DEC"). The slight DEC mismatch between UVW (live pt_dec) and `phase_center_app_dec` (nearest cached) is at most 0.125° = grid step / 2; cal pipeline tolerates this. |
| D11 | Operator-approval marker scheme | `bench/reports/<UTC>/<run_id>/<M_label>/m_operator_approved.yaml` with fields `{operator, approval_utc_iso, milestone, voltage_run_id, viz_artifact_sha256}`. The presence of this file is what `tools/dod/M2.sh::operator_approval` checks. | Reuses M0/M1 status-JSON convention pattern; sidecar yaml in the same report dir is the natural place. |
| D12 | Cal source for synth tests | Inline-built per-test (no etcd / no cache files); deterministic via `np.random.default_rng(seed=...)` | Matches plan §8 line 2167 "deterministic synthetic inputs with `np.random.default_rng(seed=...)`". |
| D13 | Conda-env split for M2 | **`dsa110-rt`** runs: `services/corr_slow_compute.py`, `bench/replay_voltage_dump.py`, `tests/test_slow_corr_synth.py`, `tools/dod/M2*.sh`, `tools/viz/corr_imager_dedisperser_check.py`, `bench/voltage_fixture_slow_corr.py`. **`casa38`** runs: `tools/build_fstable_cache.py` (calls `dsamfs.fringestopping.generate_fringestopping_table` IN-CASA38, no casa38-side edits) and `meridian_fringestop` itself (UNTOUCHED). M2.sh orchestrates: corr_slow + replay + viz in `dsa110-rt`; calls `build_fstable_cache` via casa38 python wrapper; `meridian_fringestop` runs as legacy. | h01 inventory shows `dsacalib`, `dsamfs`, `antpos`, `casacore` only installed in `casa38` env. The legacy `meridian_fringestop` already runs in `casa38`; M2 inherits the env split. **User constraint 2026-05-04**: NO edits to existing casa38 repos / installs. F14 below tracks the plan-prose update. |
| D14 | dsamfs/routines.py patch | **DROPPED — user-provided constraint (2026-05-04)**. `dsamfs/routines.py` is NOT patched; `meridian_fringestop` runs exactly as the legacy install. M2's `tools/build_fstable_cache.py` produces a per-DEC grid of `.npz` tables but how `meridian_fringestop` consumes them is operator-side / out-of-scope for this revamp. Plan §8 line 2165 ("minimal patch to `dsamfs/routines.py`...") is dropped from M2 deliverables. The plan §8 line 2171 DoD line "writes a UVH5 file with `phase_center_app_dec` = nearest cached DEC" simplifies to "writes a UVH5 file" (no nearest-cached requirement; `phase_center_app_dec` continues to be the live etcd `pt_dec` via legacy `dsamfs.routines`). | User pivot 2026-05-04. Eliminates the Subagent-C-flagged complications around `dsamfs/utils.py::load_visibility_model`'s strict `dec_rad` assertion + non-editable casa38 install. |

## Provenance

- **Subagent A** — Legacy slow-corr code survey (id `90319675`, 2026-05-04 21:30Z) — pinned `dsaX_bfCorr.cu` corr-mode as canonical; produced (i, j) → bls_idx formula; flagged cal-not-applied legacy gotcha + 2-vs-4-hand pol gotcha.
- **Subagent B** — `psrdada-python` Reader/Writer + fada/bada layout survey (id `fd6a8b3b`, 2026-05-04 21:30Z) — pinned API; flagged config_corr.yaml buffer-size mismatch (F6); supplied fada-read + bada-write Python skeleton.
- **Subagent C** — `dsamfs/routines.py` + fstable cache survey (id `fc5667ab`, 2026-05-04 21:30Z) — pinned `(nint, n_bl)` `bw` table layout; flagged compute-if-missing-actually-in-utils.py (F5) + filename precision (F7).
- **Subagent D** — Voltage-fixture replay + viz survey (id `0a5e56dd`, 2026-05-04 21:30Z) — confirmed F8 (replay PSRDADA writer not implemented), F9 (no viz tool), F13 (no operator-approval scheme).
- **Cross-checks** — F11 (per-fixture math), F12 (bada dtype) from layered subagent reports.

This file is committed at the start of M2 and consumed by Chunk 8 (hardening). It is
deleted after the plan is patched and re-locked — its job is to prevent the F1-F13
fixes from being lost between chunks.
